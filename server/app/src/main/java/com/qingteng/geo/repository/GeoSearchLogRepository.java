package com.qingteng.geo.repository;

import com.qingteng.geo.entity.GeoSearchLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeoSearchLogRepository extends JpaRepository<GeoSearchLog, Long> {
}